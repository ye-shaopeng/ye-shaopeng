# 组织：层级

- N叉树：节点 = 元素\[+ 1个父节点\] + N个子节点 ^2a24a3
    - **每个节点，除了根节点，有且仅有一个父节点。**^[=> 每个节点，到根节点的路径，有且仅有一条]
    - 根节点`root`：没有父节点 && 起始节点
    - 叶子节点：没有子节点。
- 节点的深度/层级（基于0）：从该节点，到根节点的路径，累加父节点的个数。
- 树的高度（基于1）：
    - `root == null`：0
    - `root != null`：最大的节点的深度 + 1
