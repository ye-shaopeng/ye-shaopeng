- 相同类型、固定个数 => 连续的[^1]

# 开辟一块*连续的*内存空间，存储*相同类型*、*固定个数*的元素

存储所有元素，只要（*集中*在）*一个*对象。

- => 容量固定
    - 无法保证，数组对象之后的内存空间，是空闲的[^2] => **手动扩容**
- => 元素的内存地址 = 首元素的内存地址 + 元素索引^[前面有几个元素。] * 元素长度 ^e5f8b4

[^1]: 例如`int[] array = new int[10];` => 内存空间的占用大小
[^2]: 对象的创建：“见缝插针”。
