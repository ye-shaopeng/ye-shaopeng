# 第一定律：`!(A && B) == (!A || !B)`

- `!(A && B)`：考虑，除了`A && B`，所有情况： #tolink
    - ① `!A && B`
    - ② `!A && !B`
    - ③ `A && !B`
- 具体使用：
    - `if (A && B) { ... } else { if (!A) { ... } else { ... } }`
    - `while (A && B) { ... } if (!A) { ... } else { ... }`
