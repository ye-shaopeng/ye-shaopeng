# 形式

- 初始化 -> (判断 -> 操作 -> 更新) -> (判断 -> 操作 -> 更新)...
    - `for`循环：操作终点具体（索引、引用、数值）。
    - `while`循环：操作终点不具体 || 灵活
- 初始化 -> ~~判断~~ -> (*操作* -> 更新 -> 判断) -> (操作 -> 更新 -> 判断)...
    - `do while`循环：初始化的操作单元，肯定满足循环条件。
