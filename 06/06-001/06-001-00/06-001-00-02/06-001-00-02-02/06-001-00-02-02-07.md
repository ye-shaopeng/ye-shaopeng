# linux文献：常用命令-用户组管理类

- `groupadd 组名`：创建新组。
- `cat /etc/group`：打印所有的组的信息。
- `groupdel 组名`：删除空的组。
- `groupmod -n 新组名 旧组名`：修改组名。
    - 能否`groupmod 旧组名 -n 新组名`？
- `usermod -g 组名 用户名`：修改用户的所属组。
    - 能否`usermod 用户名 -g 组名`？

---

- 视频day02：11
