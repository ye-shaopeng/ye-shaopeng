# linux文献：常用命令-用户组管理类

- `groupadd GROUP_NAME`：创建新组。
- `cat /etc/group`：打印所有的组的信息。
- `groupdel GROUP`：删除空的组。
- `groupmod -n NEW_GROUP_NAME GROUP`：修改组名。
- `usermod -g NEW_GROUP USER`：修改用户的所属组。

---

- 视频day02：11
