# linux文献：常用命令-压缩解压缩类

- 压缩的好处：1）节省存储空间；2）节省I/O。
- `tar -cz PATH ... -f TAR_NAME.tar.gz -v`：打包并压缩。
    - `-c`：打包。
    - `-z`：压缩或解压缩。
    - `-f`：包名。
    - `-v`：打印信息。
- `tar -f TAR_NAME.tar.gz -zx -C DIR -v`：解压缩并解包。
    - `-x`：解包。
    - `-C`：解到指定目录。

---

- 视频day02：16
