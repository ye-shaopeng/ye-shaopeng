# Log template

```javascript
<%*
const dailyNoteFormat = "Y-MM-DD";
if (log.mode == "WORK" && log.finished) {
    tR = `- [x] %% ${log.begin.format(dailyNoteFormat)} %%[${log.begin.format("HH:mm")} - ${log.end.format("HH:mm")}] [波莫多罗] [${log.duration}分钟]`;
}
%>
```
